{
  "_comment": "Hand-maintained reachability notes, merged into ui-strings.csv as the `note` column. Reachability can't be derived mechanically — each entry below was traced by hand. Anything marked NEVER SHOWN is not worth rewriting.",
  "TieBreakPanel.3f67df": "NEVER SHOWN — needs both players' picks in one state, but the server resolves the tie in the same tick as the second pick, before broadcasting",
  "GameOverScreen.01955d": "NEVER SHOWN — exit resigns and then leaves, so the resigner is already back on the home screen; only the opponent sees a resignation result",
  "errorMessages.bdeea9": "NEVER SHOWN — in-game error; errorMessage only renders on the home screen",
  "errorMessages.3d9b53": "NEVER SHOWN — in-game error, and the board is unclickable off-turn so it can't even be sent",
  "errorMessages.096960": "NEVER SHOWN — in-game error, and the board is unclickable mid-fight so it can't even be sent",
  "errorMessages.a7ea45": "NEVER SHOWN — in-game error; errorMessage only renders on the home screen",
  "errorMessages.ae8ade": "NEVER SHOWN — in-game error, and the hand buttons disable after picking so it can't even be sent",
  "errorMessages.d20ef7": "NEVER SHOWN — in-game error; errorMessage only renders on the home screen",
  "errorMessages.5c9f53": "NEVER SHOWN — in-game error; errorMessage only renders on the home screen",
  "errorMessages.79f0ee": "NEVER SHOWN — the client never sends a game action without a seat",
  "errorMessages.2e3d44": "NEVER SHOWN — the client never joins twice on one socket",
  "errorMessages.b9aa84": "NEVER SHOWN — the client always sends valid JSON",
  "SetupScreen.5755ef": "WRONG, and shown — says the game continues without you, but exit now resigns and ends it. Compare GameBoard's version",
  "App.ea5cb4": "Barely seen — a bot room leaves the lobby almost immediately",
  "SetupScreen.b0d0f9": "Barely seen — both-ready state lasts an instant",
  "characterAssets.a49277": "Tooltip / screen reader only (img title + alt), not visible text",
  "characterAssets.557e36": "Tooltip / screen reader only (img title + alt), not visible text",
  "characters.c0e65a": "Tooltip / screen reader only (img title + alt), not visible text",
  "HomeScreen.552471": "Screen reader only (aria-label on the modal's X)",
  "App.ee7958": "Screen reader only (aria-label on the room code)",
  "HomeScreen.92d0d1": "Screen reader only (logo alt text)",
  "ExitButton.b93906": "Visible label AND the button's aria-label"
}
