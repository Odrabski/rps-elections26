# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# TS build info
*.tsbuildinfo
.env
*.env
config.json
.DS_Store
node_modules/

# Source art kept locally for re-exporting. The game only ever serves the .webp conversions in
# client/public/assets, so the originals don't belong in the repo — they were ~113 MB of it.
/*.png
.sfx-sources/

# Loose source audio dropped at the root for picking clips out of. The ones actually used are
# copied into sfx-src/, which is committed; the packs themselves are large and re-downloadable.
/Audio/

# Piper TTS: a 63MB model and its venv, both rebuildable via tools/setup-piper.sh
.piper-venv/
